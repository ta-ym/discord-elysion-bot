# 💰 給与システム使用ガイド

## 📋 一斉給与支給の手順

### 1️⃣ 事前確認（プレビューモード）
```bash
/salary-bulk preview: true month: 2024-11
```

**プレビュー結果で確認できること：**
- 📊 総対象者数と支給予定額
- 👥 支給対象者リスト
- ✅ 支給済みユーザー
- ❌ エラーユーザー（権限不足等）
- 🏷️ 活動中の給与ロール

### 2️⃣ 実際の支給実行
```bash
/salary-bulk preview: false month: 2024-11
```

**実行時の処理：**
- 💰 対象者全員に自動支給
- 📝 支給記録の作成
- 💳 残高への反映
- 📊 実行結果の詳細レポート

### 3️⃣ 支給済み状態のリセット（必要時）
```bash
/salary-rollback-all confirmation: CONFIRM_ROLLBACK_ALL month: 2024-11
```

**ロールバック処理：**
- 🔄 指定月の全支給記録を削除
- 💸 支給分の残高を差し引き
- 📋 処理結果の詳細表示
- ⚠️ 安全のため確認文字列が必要

## 🎯 使用例

### 毎月の給与支給フロー
1. **月初に事前確認**
   ```
   /salary-bulk preview: true
   ```
   
2. **内容確認後に実支給**
   ```
   /salary-bulk preview: false
   ```

3. **誤支給時の緊急対応**
   ```
   /salary-rollback-all confirmation: CONFIRM_ROLLBACK_ALL
   ```

### トラブルシューティング

**Q: プレビューで想定より対象者が少ない**
- A: 給与ロール設定を確認（`/salary-config list`）

**Q: 支給済みユーザーが多い**
- A: 前回の支給をリセット（`/salary-rollback-all`）

**Q: エラーユーザーがいる**
- A: 詳細ボタンでエラー原因を確認

## 🔧 関連コマンド

- `/salary-config` - 給与ロール設定
- `/salary-history` - 個人給与履歴
- `/salary-details` - 詳細な支給情報
- `/salary-rollback` - 個人ロールバック

## ⚠️ 注意事項

- **プレビューは必須**: 実支給前に必ず事前確認を行う
- **月単位管理**: 給与は月単位で管理される
- **重複防止**: 同月内の重複支給は自動的に防止される
- **権限管理**: 管理者権限が必要な操作がある